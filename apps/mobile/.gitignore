.expo/
# Anchored, so these match only the prebuild output at the root of the app.
# Unanchored they match any directory of that name at any depth, which silently
# swallowed `modules/face-track/ios/` — the Swift implementation and every
# generated iOS binding — leaving a native module that could not build from a
# fresh clone.
/ios/
/android/
.env.local

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli