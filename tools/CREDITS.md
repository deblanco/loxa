# tools/

Standalone scripts. Not a workspace, not typechecked in CI, and nothing here
ships.

Currently empty. The Google Cloud setup that used to live here as a script is
done, and is described in CLAUDE.md instead — a runbook for a thing that has
already happened is a script nobody will ever run again.
